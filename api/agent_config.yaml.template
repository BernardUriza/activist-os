# agent_config.yaml — Band agent credentials for Activist OS
# Register each agent at app.band.ai/agents → New Agent → External Agent
# Copy the UUID (agent_id) and API key into the blocks below.
# Add this file to .gitignore — it contains live API keys.

evidence:
  agent_id: "PASTE-UUID-FROM-BAND-UI"
  api_key: "PASTE-API-KEY-FROM-BAND-UI"

campaign:
  agent_id: "PASTE-UUID-FROM-BAND-UI"
  api_key: "PASTE-API-KEY-FROM-BAND-UI"

safety:
  agent_id: "PASTE-UUID-FROM-BAND-UI"
  api_key: "PASTE-API-KEY-FROM-BAND-UI"

outreach:
  agent_id: "PASTE-UUID-FROM-BAND-UI"
  api_key: "PASTE-API-KEY-FROM-BAND-UI"

coordinator:
  agent_id: "PASTE-UUID-FROM-BAND-UI"
  api_key: "PASTE-API-KEY-FROM-BAND-UI"

reporter:
  agent_id: "PASTE-UUID-FROM-BAND-UI"
  api_key: "PASTE-API-KEY-FROM-BAND-UI"
