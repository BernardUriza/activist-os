[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "contract: canonical-invariant contract test (RED until the implementation lands in Step 3)",
]
